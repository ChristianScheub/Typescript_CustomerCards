import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.scheub.customercards',
  appName: 'de.scheub.customercards',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
  
};

export default config;
